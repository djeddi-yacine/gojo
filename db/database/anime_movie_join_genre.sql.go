// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.25.0
// source: anime_movie_join_genre.sql

package db

import (
	"context"
)

const getAnimeMovieGenresDirectly = `-- name: GetAnimeMovieGenresDirectly :many
SELECT genres.id, genres.genre_name, genres.created_at
FROM genres
JOIN anime_movie_genres ON genres.id = anime_movie_genres.genre_id
WHERE anime_movie_genres.anime_id = $1
`

func (q *Queries) GetAnimeMovieGenresDirectly(ctx context.Context, animeID int64) ([]Genre, error) {
	rows, err := q.db.Query(ctx, getAnimeMovieGenresDirectly, animeID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Genre{}
	for rows.Next() {
		var i Genre
		if err := rows.Scan(&i.ID, &i.GenreName, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
