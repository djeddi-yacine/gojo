// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.25.0
// source: anime_movie_server_sub_torrent.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAnimeMovieServerSubTorrent = `-- name: CreateAnimeMovieServerSubTorrent :one
INSERT INTO anime_movie_server_sub_torrents (server_id, torrent_id)
VALUES ($1, $2)
RETURNING id, server_id, torrent_id, created_at
`

type CreateAnimeMovieServerSubTorrentParams struct {
	ServerID  int64
	TorrentID int64
}

func (q *Queries) CreateAnimeMovieServerSubTorrent(ctx context.Context, arg CreateAnimeMovieServerSubTorrentParams) (AnimeMovieServerSubTorrent, error) {
	row := q.db.QueryRow(ctx, createAnimeMovieServerSubTorrent, arg.ServerID, arg.TorrentID)
	var i AnimeMovieServerSubTorrent
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.TorrentID,
		&i.CreatedAt,
	)
	return i, err
}

const deleteAnimeMovieServerSubTorrent = `-- name: DeleteAnimeMovieServerSubTorrent :exec
DELETE FROM anime_movie_server_sub_torrents
WHERE id = $1
`

func (q *Queries) DeleteAnimeMovieServerSubTorrent(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteAnimeMovieServerSubTorrent, id)
	return err
}

const getAnimeMovieServerSubTorrent = `-- name: GetAnimeMovieServerSubTorrent :one
SELECT id, server_id, torrent_id, created_at FROM anime_movie_server_sub_torrents
WHERE id = $1
LIMIT 1
`

func (q *Queries) GetAnimeMovieServerSubTorrent(ctx context.Context, id int64) (AnimeMovieServerSubTorrent, error) {
	row := q.db.QueryRow(ctx, getAnimeMovieServerSubTorrent, id)
	var i AnimeMovieServerSubTorrent
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.TorrentID,
		&i.CreatedAt,
	)
	return i, err
}

const listAnimeMovieServerSubTorrents = `-- name: ListAnimeMovieServerSubTorrents :many
SELECT id, server_id, torrent_id, created_at FROM anime_movie_server_sub_torrents
WHERE server_id = $1
ORDER BY id
`

func (q *Queries) ListAnimeMovieServerSubTorrents(ctx context.Context, serverID int64) ([]AnimeMovieServerSubTorrent, error) {
	rows, err := q.db.Query(ctx, listAnimeMovieServerSubTorrents, serverID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AnimeMovieServerSubTorrent{}
	for rows.Next() {
		var i AnimeMovieServerSubTorrent
		if err := rows.Scan(
			&i.ID,
			&i.ServerID,
			&i.TorrentID,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateAnimeMovieServerSubTorrent = `-- name: UpdateAnimeMovieServerSubTorrent :one
UPDATE anime_movie_server_sub_torrents
SET 
  server_id = COALESCE($1, server_id),
  torrent_id = COALESCE($2, torrent_id)
WHERE
  id = $3
RETURNING id, server_id, torrent_id, created_at
`

type UpdateAnimeMovieServerSubTorrentParams struct {
	ServerID  pgtype.Int8
	TorrentID pgtype.Int8
	ID        int64
}

func (q *Queries) UpdateAnimeMovieServerSubTorrent(ctx context.Context, arg UpdateAnimeMovieServerSubTorrentParams) (AnimeMovieServerSubTorrent, error) {
	row := q.db.QueryRow(ctx, updateAnimeMovieServerSubTorrent, arg.ServerID, arg.TorrentID, arg.ID)
	var i AnimeMovieServerSubTorrent
	err := row.Scan(
		&i.ID,
		&i.ServerID,
		&i.TorrentID,
		&i.CreatedAt,
	)
	return i, err
}
