// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.23.0
// source: anime_season_short_title.sql

package db

import (
	"context"
)

const createAnimeSeasonShortTitle = `-- name: CreateAnimeSeasonShortTitle :one
INSERT INTO anime_season_short_titles (season_id, title_text)
VALUES ($1, $2)
ON CONFLICT (season_id, title_text)
DO UPDATE SET 
    season_id = excluded.season_id
RETURNING id, season_id, title_text, created_at
`

type CreateAnimeSeasonShortTitleParams struct {
	SeasonID  int64
	TitleText string
}

func (q *Queries) CreateAnimeSeasonShortTitle(ctx context.Context, arg CreateAnimeSeasonShortTitleParams) (AnimeSeasonShortTitle, error) {
	row := q.db.QueryRow(ctx, createAnimeSeasonShortTitle, arg.SeasonID, arg.TitleText)
	var i AnimeSeasonShortTitle
	err := row.Scan(
		&i.ID,
		&i.SeasonID,
		&i.TitleText,
		&i.CreatedAt,
	)
	return i, err
}

const deleteAnimeSeasonShortTitle = `-- name: DeleteAnimeSeasonShortTitle :exec
DELETE FROM anime_season_short_titles
WHERE id = $1
`

func (q *Queries) DeleteAnimeSeasonShortTitle(ctx context.Context, id int64) error {
	_, err := q.db.Exec(ctx, deleteAnimeSeasonShortTitle, id)
	return err
}

const getAnimeSeasonShortTitles = `-- name: GetAnimeSeasonShortTitles :many
SELECT id, season_id, title_text, created_at FROM anime_season_short_titles
WHERE season_id = $1
`

func (q *Queries) GetAnimeSeasonShortTitles(ctx context.Context, seasonID int64) ([]AnimeSeasonShortTitle, error) {
	rows, err := q.db.Query(ctx, getAnimeSeasonShortTitles, seasonID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AnimeSeasonShortTitle{}
	for rows.Next() {
		var i AnimeSeasonShortTitle
		if err := rows.Scan(
			&i.ID,
			&i.SeasonID,
			&i.TitleText,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const queryAnimeSeasonShortTitles = `-- name: QueryAnimeSeasonShortTitles :many
WITH search_documents AS (
  SELECT
    season_id,
    title_text,
    to_tsvector('pg_catalog.english', title_text) AS title_text_tsv
  FROM anime_season_short_titles
)
SELECT season_id
FROM search_documents
WHERE (
  $1::varchar IS NOT NULL AND $1::varchar <> '' AND
  (
    SELECT bool_and(
      to_tsvector('pg_catalog.english', lower(translate(title_text, '[:punct:]', ''))) 
      @@ plainto_tsquery(word)
    )
    FROM unnest(regexp_split_to_array($1::varchar, '\\W+')) AS word
  )
  OR title_text ILIKE '%' || $1::varchar || '%'
)
ORDER BY
  ts_rank(title_text_tsv, phraseto_tsquery($1::varchar)) DESC,
  similarity(title_text, $1::varchar) DESC
  LIMIT $2
  OFFSET $3
`

type QueryAnimeSeasonShortTitlesParams struct {
	Column1 string
	Limit   int32
	Offset  int32
}

func (q *Queries) QueryAnimeSeasonShortTitles(ctx context.Context, arg QueryAnimeSeasonShortTitlesParams) ([]int64, error) {
	rows, err := q.db.Query(ctx, queryAnimeSeasonShortTitles, arg.Column1, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []int64{}
	for rows.Next() {
		var season_id int64
		if err := rows.Scan(&season_id); err != nil {
			return nil, err
		}
		items = append(items, season_id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
